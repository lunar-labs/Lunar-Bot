<%- include('blocks/header', {bot, user, path}) %>
  <div class="jumbotron jumbotron-fluid">
    <div class="container">

      <h1 class="display-4"><%= bot.user.username %> Dashboard</h1>
      <p class="lead"><% if(bot.appInfo.description.length > 3) { %> <%= bot.appInfo.description %> <% } else { %>No description given.<% }%></p>
      <p><a class="btn btn-primary" href="/commands" role="button">Command Help »</a></p>
  </div>
  </div>
<% include ./blocks/footer %>
